---
title: 'Admin - Booking Management'
description: 'Manage and monitor all bookings across the platform'
api: 'GET /api/admin/bookings'
---

<Warning>
  All admin booking endpoints require ADMIN role. Unauthorized requests will return 403 Forbidden.
</Warning>

## List All Bookings

Retrieve a paginated list of all bookings across the platform with full details.

<CodeGroup>
```bash cURL
curl -X GET 'https://api.sportshub.com/api/admin/bookings?page=0&size=20' \
  -H 'Authorization: Bearer YOUR_JWT_TOKEN'
```

```javascript JavaScript
const response = await fetch('https://api.sportshub.com/api/admin/bookings?page=0&size=20', {
  headers: {
    'Authorization': 'Bearer YOUR_JWT_TOKEN'
  }
});
const bookings = await response.json();
```

```python Python
import requests

response = requests.get(
    'https://api.sportshub.com/api/admin/bookings',
    params={'page': 0, 'size': 20},
    headers={'Authorization': 'Bearer YOUR_JWT_TOKEN'}
)
bookings = response.json()
```
</CodeGroup>

### Query Parameters

<ParamField query="page" type="integer" default="0">
  Page number for pagination (0-indexed)
</ParamField>

<ParamField query="size" type="integer" default="20">
  Number of bookings per page
</ParamField>

### Response

<ResponseField name="id" type="UUID">
  Unique booking identifier
</ResponseField>

<ResponseField name="resourceId" type="UUID">
  ID of the booked resource (court, field, etc.)
</ResponseField>

<ResponseField name="playerId" type="UUID">
  ID of the user who made the booking
</ResponseField>

<ResponseField name="bookingDate" type="date">
  Date of the booking (YYYY-MM-DD)
</ResponseField>

<ResponseField name="startTime" type="time">
  Start time of the booking (HH:mm:ss)
</ResponseField>

<ResponseField name="endTime" type="time">
  End time of the booking (HH:mm:ss)
</ResponseField>

<ResponseField name="pricePaid" type="decimal">
  Amount paid for the booking
</ResponseField>

<ResponseField name="currency" type="string">
  Currency code (e.g., USD, EUR)
</ResponseField>

<ResponseField name="status" type="string">
  Booking status: `PENDING`, `CONFIRMED`, `CANCELLED`, `COMPLETED`, `NO_SHOW`
</ResponseField>

<ResponseField name="paymentStatus" type="string">
  Payment status: `PENDING`, `PAID`, `REFUNDED`, `FAILED`
</ResponseField>

<ResponseField name="cancelledAt" type="timestamp">
  Timestamp when booking was cancelled (null if not cancelled)
</ResponseField>

<ResponseField name="cancelReason" type="string">
  Reason for cancellation (null if not cancelled)
</ResponseField>

<ResponseField name="createdAt" type="timestamp">
  Booking creation timestamp
</ResponseField>

<ResponseField name="updatedAt" type="timestamp">
  Last update timestamp
</ResponseField>

<ResponseField name="expiresAt" type="timestamp">
  Expiration time for pending bookings
</ResponseField>

<ResponseField name="resourceName" type="string">
  Name of the booked resource
</ResponseField>

<ResponseField name="venueName" type="string">
  Name of the venue
</ResponseField>

<ResponseField name="venueCity" type="string">
  City where the venue is located
</ResponseField>

### Example Response

```json
[
  {
    "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "resourceId": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
    "playerId": "c3d4e5f6-a7b8-9012-cdef-123456789012",
    "bookingDate": "2026-03-15",
    "startTime": "14:00:00",
    "endTime": "15:30:00",
    "pricePaid": 45.00,
    "currency": "USD",
    "status": "CONFIRMED",
    "paymentStatus": "PAID",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-03-08T10:30:00Z",
    "updatedAt": "2026-03-08T10:35:00Z",
    "expiresAt": null,
    "resourceName": "Court 1",
    "venueName": "Elite Padel Club",
    "venueCity": "Miami"
  }
]
```

---

## Cancel Booking

Administratively cancel a booking with a specified reason.

<CodeGroup>
```bash cURL
curl -X PATCH 'https://api.sportshub.com/api/admin/bookings/{bookingId}/cancel' \
  -H 'Authorization: Bearer YOUR_JWT_TOKEN' \
  -H 'Content-Type: application/json' \
  -d '{
    "reason": "Venue maintenance required"
  }'
```

```javascript JavaScript
const response = await fetch(
  `https://api.sportshub.com/api/admin/bookings/${bookingId}/cancel`,
  {
    method: 'PATCH',
    headers: {
      'Authorization': 'Bearer YOUR_JWT_TOKEN',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      reason: 'Venue maintenance required'
    })
  }
);
const cancelledBooking = await response.json();
```

```python Python
import requests

response = requests.patch(
    f'https://api.sportshub.com/api/admin/bookings/{booking_id}/cancel',
    headers={
        'Authorization': 'Bearer YOUR_JWT_TOKEN',
        'Content-Type': 'application/json'
    },
    json={'reason': 'Venue maintenance required'}
)
cancelled_booking = response.json()
```
</CodeGroup>

### Path Parameters

<ParamField path="bookingId" type="UUID" required>
  The unique identifier of the booking to cancel
</ParamField>

### Request Body

<ParamField body="reason" type="string" required>
  Reason for administrative cancellation. This will be visible to the user.
</ParamField>

### Response

Returns the updated `BookingResponse` object with:
- `status` set to `CANCELLED`
- `cancelledAt` timestamp populated
- `cancelReason` containing the provided reason

<Warning>
  Admin cancellations may trigger automatic refunds depending on the cancellation policy. Users will be notified of the cancellation via email.
</Warning>

## Common Admin Tasks

### Monitor Booking Activity

Use the list endpoint with pagination to:
- Review recent bookings
- Identify problematic patterns
- Monitor payment status issues
- Track cancellation rates

### Handle Disputes

Admins can cancel bookings to resolve:
- Venue emergencies or maintenance
- Customer disputes
- Payment issues
- Resource availability conflicts

### Generate Reports

Booking data can be used for:
- Revenue analysis
- Resource utilization reports
- Peak time identification
- Venue performance metrics
